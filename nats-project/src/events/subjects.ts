// this is the list of all the channels
// whenever we say the value is type Subjects, it has to have one of teh values which are defined inside it
export enum Subjects {
  ArtifactCreated =  'artifact:created',
  OrderUpdated = 'order:updated'
} 